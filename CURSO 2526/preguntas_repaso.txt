PREGUNTAS TEMA DDL DDM.

1 Explica con tus palabras una situación en la que usarías un Statement y otra en la que usarías un PreparedStatement. No escribas código, solo describe el caso de uso.

2 Te dan esta consulta:
SELECT * FROM alumnos WHERE edad > ?
Indica dos ventajas concretas de usarla con PreparedStatement en lugar de construir el SQL con concatenaciones de cadenas.

3 Imagina que en una aplicación se repite muchas veces la misma consulta con distintos parámetros. ¿Qué impacto tiene eso en rendimiento si usas Statement frente a PreparedStatement? Razona brevemente.

4 Relaciona cada frase con el tipo correcto (marca S o PS y explica por qué en una línea):

“Permite que el motor de la BD reutilice el plan de ejecución.”

“Construye la consulta completa como un String en tiempo de ejecución.”

“Ayuda a evitar inyección SQL si se usa correctamente.”

5 Supón que tienes una interfaz Java con constantes String para cada nombre de columna de una tabla (por ejemplo, String COL_NOMBRE = "nombre"). Explica qué problema práctico resuelve este enfoque cuando trabajas con JDBC.

6 Te piden modificar una tabla y cambiar el nombre de varias columnas. ¿En qué ayuda tener centralizados los nombres de columna en una interfaz o clase de constantes en lugar de “quemarlos” en el código? Explica el impacto en mantenimiento.

7 En una aplicación grande, ¿qué ventaja ves en tener una clase o interfaz que representa el “esquema” (nombres de columnas, tipos, etc.) frente a ir escribiendo los nombres a mano en cada ResultSet.getXXX("columna")? Pon un ejemplo de error típico que se evita.


PREGUNTAS TEMA FILE - XML.

8 Describe el recorrido completo: desde que tienes un objeto Java en memoria hasta que lo puedes guardar en un fichero (texto, binario o XML) y volver a reconstruirlo después. Nombra al menos dos tecnologías o conceptos vistos (por ejemplo, Serializable, DOM, streams…).

9. ¿Qué diferencia conceptual ves entre:

- serializar un objeto Alumno a un .dat con ObjectOutputStream,

- generar un XML con los datos de Alumno usando DOM?
No escribas código, compáralos en términos de legibilidad, portabilidad y acoplamiento.

10. Para qué crees que sirve, en un proyecto real, poder transformar un XML con XSLT a HTML o a otro XML distinto? Pon un ejemplo de uso concreto (listado, informe, integración, etc.).


11. En la parte de ficheros, se habla de Reader/Writer frente a InputStream/OutputStream. Sin entrar en APIs, ¿qué criterio práctico usarías para decidir si trabajas con flujos de caracteres o de bytes en una aplicación real? Explica cual es la diferencia en los flujos

12. ¿Qué diferencia hay entre usar FileReader y BufferedReader en la lectura de archivos en Java? 

13. En Java, ¿cómo cargarías un archivo JSON y accederías a uno de sus atributos? Indica para que utilizarías la librería Gson en el código.


14. Si quiero leer un .txt con la siguiente linea de codigo nombre,apellido,dni donde se repite tantas veces como usuarios tenga en el fichero. ¿Como lo llevarias a una List de java?

PREGUNTAS HIBERNATE

15. En Hibernate, ¿qué diferencia hay entre el estado transient, persistent y detached de una entidad? Explica con un ejemplo práctico qué pasa con un objeto Usuario cuando lo cargas con session.get(), lo modificas y luego haces session.close() sin hacer commit.

16. Explica la diferencia práctica entre FetchType.LAZY y FetchType.EAGER en una relación @OneToMany. Si mapeas Cliente con una lista de Pedidos y usas LAZY, ¿qué problema surge al intentar iterar esa lista fuera de la sesión y cómo lo solucionas en código?

17. Explica el ciclo de vida de una Session de Hibernate. Si en un método de servicio haces session.beginTransaction(), cargas entidades, haces rollback() por excepción y luego intentas usar esas entidades, ¿qué error obtienes y por qué?

18. ¿Qué diferencia hay entre Criteria API (o CriteriaBuilder en Hibernate 5+) y HQL en consultas complejas? Escribe un ejemplo práctico de CriteriaBuilder para buscar Productos con precio > 50€ y categoría "Electrónica", explicando por qué es más seguro que concatenar strings en HQL.

19. Indica que hacer las anotaciones @Trascient @Id @Query @OnetoOne

20. Cuanto se gestionan relaciones entre clases, indicas las necesidades obligatorias y optativas entre las dos clases, identificando las direcciones

21. Si configuramos relaciones entre dos entidades, ¿Que tipos de objetos se gestionan en cada una de las relaciones @OnetoOne @ManytoMany @OnetoMany?

22. Que opción de hibernate me permite gestionar la base de datos sin necesidad de ejecutar sentencias SQL para la creación del esquema. Que diferencia hay entre update y create en dicha configuracion

23. Imagina que quieres crear una relacion @ManytoMany, si hablamos de tablas ¿que necesidades tendríamos en la base de datos?

24. ¿Cual es la diferencia entre hacer una query mediante CriteraQuery HQL o NamedQuery?

25. Explica la donde entra hibernate en la infraestructura de un sistema que ealiza transacciones contra la base de datos. Que archivos de configuración son esenciales y que es lo que se indica en ellos.

SPRING DATA

26. Explica la diferencia entre definir consultas mediante convenciones de nombres de métodos (ej. findByName) y usando la anotación @Query con JPQL en un repositorio JpaRepository.

27. Dónde entra Spring Data en la infraestructura de un sistema con transacciones contra BD? ¿Qué anotaciones y archivos de configuración son esenciales (ej. application.properties), y qué se indica en ellos?

29. ¿Cual es la diferencia entre un servicio, un repositorio y un modelo?

30. Cuando hablamos de controlador, ¿a que nos referimos? Indica cual es el cometido principal de un controlador, las anotaciones que debe tener y para que se utilzan. Es necesario un controlador por modelo?

31. Indica que hace exáctamente la notacion @Autowired cuando queremos iniciar una variable.

32. Indica cual es la estructura de una URL para que esta pueda ser procesada por el servidor TOMCAT levantado en Spring. Indica cuales son las diferentes posibilidades a la hora de pasar parámetros en una ruta